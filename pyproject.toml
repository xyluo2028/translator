[project]
name = "llm-translator"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = []

[project.optional-dependencies]
transformers = [
  "accelerate>=1.5.0",
  "sentencepiece>=0.2.0",
  "torch>=2.4.0",
  "transformers>=4.57.0",
]

[tool]
# No tooling configured yet; keep MVP dependency-free.
